import React from "react";

export default function page({ params }: { params: { caseId: string } }) {
  // TODO: Can Only see case if you are part of the case or the case is archived
  return <div>Home</div>;
}
